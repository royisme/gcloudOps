- Syntax check scripts:
  - `bash -n install.sh`
  - `bash -n scripts/host-layout-init.sh`
  - `bash -n scripts/host-bootstrap.sh`
  - `bash -n scripts/host-selfheal.sh`
- Run installer:
  - `sudo bash install.sh`
- Run bootstrap:
  - `sudo bash /srv/ops/gcloudOps/scripts/host-bootstrap.sh`
- Run self-heal manually:
  - `sudo bash /srv/ops/gcloudOps/scripts/host-selfheal.sh`
- Systemd operations:
  - `sudo systemctl daemon-reload`
  - `sudo systemctl enable --now host-selfheal.service`
  - `sudo systemctl status host-selfheal.service`
- Basic navigation/utilities on macOS host shell: `ls`, `cd`, `find`, `rg`, `cat`, `git`.