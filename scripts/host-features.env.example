# Feature flags for host bootstrap and self-heal checks.
# Copy to /etc/hostops/features.env and adjust values as needed.
#
# true/false (case-insensitive) are supported.

# Core container runtime checks and setup.
ENABLE_DOCKER=true

# GPU driver/toolkit checks and setup.
# Keep false for generic non-AI/non-GPU workloads.
ENABLE_GPU=false

# Run service-specific hooks after host-level checks.
ENABLE_SERVICE_HOOKS=true

# Hook discovery path and execution controls.
SERVICE_HOOKS_DIR=/srv/ops/gcloudOps/services-enabled
SERVICE_HOOK_TIMEOUT_SEC=300
SERVICE_HOOK_FAIL_MODE=continue
